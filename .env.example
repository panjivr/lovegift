# Supabase (default mode). Leave blank to run in URL-only mode (no DB).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE=        # server-side photo upload

# Simple gate for /admin
ADMIN_KEY=ganti-aku

# Used to build share links (e.g. https://lovegift.vercel.app)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
